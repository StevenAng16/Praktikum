

export default function Header ({text}) {
    return (
        <div style={{background:"turquoise", color: "snow", padding:"10px 50px", marginBottom: 20}}>
            <h1>{text}</h1>
        </div>
    )
}